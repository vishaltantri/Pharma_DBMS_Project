💊 Pharmacy Management System (Oracle + Python)

An interactive pharmacy management system built using Python (oracledb/cx_Oracle) and Oracle Database.
This project demonstrates a realistic, database-driven system with tables, triggers, procedures, and reporting features.

🚀 Features

Supplier, Customer, and Medicine Management

Inventory tracking (with low-stock and expiry detection)

Sales and billing with stored procedures

Real-time audit logs via database triggers

Reports using SQL views, aggregates, subqueries, and joins

Interactive CLI menus for easy use

Schema setup, seeding, and maintenance tools included
